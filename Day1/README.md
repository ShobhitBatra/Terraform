# Terraform + AWS

## Day 1: Creating EC2 + key_pair + SG + basic user_data script

This project demonstrates how to provision a **basic EC2 instance** on AWS using **Terraform**, including:

- AWS Key Pair
- Security Group with inbound & outbound rules
- EC2 instance
- Basic `user_data` script to install and start Nginx

This is a **beginner-friendly Terraform project** intended for learning and practice.

---

## 🧱 Resources Created

- **AWS Key Pair**
- **AWS Security Group**
  - Inbound: SSH (22), HTTP (80)
  - Outbound: All traffic
- **EC2 Instance**
  - Amazon Linux / Ubuntu AMI
  - `t2.micro` instance type
  - Nginx installed via `user_data`

---

## 📁 Project Structure
```
.
├── main.tf
├── userdata.sh
└── README.md
```
---

## 🔑 Prerequisites

Before running this project, ensure you have:

- An AWS account
- AWS CLI installed
- Terraform installed
- An existing SSH key pair (public key)

## ⚙️ AWS Configuration

### Step 1: Configure AWS CLI

Run the following command:

```bash
aws configure

Provide:

AWS Access Key ID

AWS Secret Access Key

Default region (e.g. ap-south-1)

Output format (json)
```

### Step 2: SSH Key Setup

Make sure your public key exists at:

```bash
~/.ssh/terraform.pub
```

If not, create one:

```bash
ssh-keygen -t rsa -b 4096 -f ~/.ssh/terraform
```

## 🚀 Terraform Execution Steps

### Step 1: Initialize Terraform
```bash
terraform init
```
### Step 2: Format the Code
```bash
terraform fmt
```
### Step 3: Validate Configuration
```bash
terraform validate
```
### Step 4: Review Execution Plan
```bash
terraform plan
```
### Step 5: Apply Configuration
```bash
terraform apply
```
Type yes when prompted.

## 🌐 Verify Deployment

- Get the EC2 public IP from AWS Console
- Open browser:
```bash
http://<EC2_PUBLIC_IP>
```
- You should see the Nginx welcome page

## 🧹 Cleanup (Important)

To avoid AWS charges:
```bash
terraform destroy
```

## 📌 Notes

- Default VPC is used
- Security Group allows public SSH and HTTP access
- This project is intended for learning purposes only

## 📚 Learning Outcome

- By completing this project, you learn:
- Terraform resource creation
- AWS EC2 basics
- Security Groups & rules
- User data automation
- Terraform workflow (init → plan → apply)

## Future scope 

- Using variable for: 
   - AMI ID
   - Instance type
   - Region
- Modules and etc 

## 🚫 Files Not to be Committed to GitHub

The following files and directories are intentionally excluded using `.gitignore`:

- **`terraform.tfstate`**
- **`terraform.tfstate.backup`**
- **`.terraform/` directory**

### ❓ Why are these excluded?

- These files contain **sensitive infrastructure details** such as resource IDs, IP addresses, and metadata(security risk).
- They are **auto-generated by Terraform** during `terraform init` and `terraform apply`.
- Terraform can **recreate them locally**, so committing them is unnecessary and unsafe.


## ⭐ Author

**Shobhit Batra** <br>
**Learning Terraform & Cloud | DevOps Enthusiast**